[package]
name = "noob-electrical-components-remote-cutoff-triode"
description = "A physical model of the remote-cutoff triode, the gain element of a variable-mu limiter: an anode-current law with a parameter set per valve type, and nothing of the circuit around it"
readme = "README.md"
version.workspace = true
authors.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
# Nothing here goes to crates.io until I say so.
publish = false

[dependencies]
